#!/usr/bin/node
let i = parseInt(process.argv[2]);
if (Number.isNaN(i)) {
  console.log('Missing size');
} else {
  while (i < i) {
    console.log('X');
    i--;
     while(j < i){
       console.log('X');
       j--;
    }
  }
}
